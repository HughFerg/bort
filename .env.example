# Bort Search - Environment Configuration
# Copy this file to .env and update values for your environment

# Database path (default: data/simpsons.lance)
DATABASE_PATH=data/simpsons.lance

# Frames directory (default: data/frames)
FRAMES_PATH=data/frames

# Server configuration
HOST=0.0.0.0
PORT=8000

# CORS origins (comma-separated, use * for development)
ALLOWED_ORIGINS=*

# Optional: Admin authentication for delete operations
# ADMIN_PASSWORD=your-secure-password-here

# Optional: External image CDN (if hosting frames on R2/S3)
# IMAGE_CDN_URL=https://your-bucket.r2.cloudflarestorage.com
