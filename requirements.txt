# Core dependencies for search API
open-clip-torch
torch
lancedb
fastapi
uvicorn[standard]
pillow
pyarrow
slowapi

# Maintenance scripts (black frame removal, character tagging)
numpy
tqdm
pandas

# Character detection model (optional - only for retagging)
ultralytics
